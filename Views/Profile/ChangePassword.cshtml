@{
    ViewBag.Title = "Change Password";
}
@* Change Password (Phase 10). js/pages/sme/changePassword.js posts directly to Controllers/
   ProfileController.cs's change-password action, which calls ASP.NET Identity's own
   UserManager.ChangePasswordAsync - no custom password system anywhere in this stack. *@
@section scripts {
    <script type="module" src="@Url.Content("~/Scripts/js/bootstrap/changePassword.js")"></script>
}
