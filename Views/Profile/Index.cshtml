@{
    ViewBag.Title = "My Profile";
}
@* Personal Profile (Phase 10) - same "thin bootstrap shell" pattern as every other real MVC
   page in this app: the actual UI is entirely JS-rendered (js/pages/sme/profile.js), mounted
   inside the persistent Dashboard shell exactly like Application Details/Tracking. *@
@section scripts {
    <script type="module" src="@Url.Content("~/Scripts/js/bootstrap/profile.js")"></script>
}
